import { SpecialIsAlphaNumericDirective } from './special-is-alpha-numeric.directive';

describe('SpecialIsAlphaNumericDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialIsAlphaNumericDirective();
    expect(directive).toBeTruthy();
  });
});
