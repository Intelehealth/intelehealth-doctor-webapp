import { FollowUpDatePipe } from './follow-up-date.pipe';

describe('FollowUpDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FollowUpDatePipe();
    expect(pipe).toBeTruthy();
  });
});
